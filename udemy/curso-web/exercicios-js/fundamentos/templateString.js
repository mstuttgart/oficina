const nome = 'Lara'

const template = `Olá ${nome} !` // Substitui pela variavel 'nome'

console.log(template)

console.log(`1 + 1 = ${1 + 1}`) // Executa qualquer codigo que esteja entre ${}. Também aceita funcoes